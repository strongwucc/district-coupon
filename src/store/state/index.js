import common_data from './common'
import user_info from './user/info'
import user_auth from './user/auth'

const common_module = {
  common_data
}

const user_module = {
  user_info,
  user_auth
}
export default {
  common_module,
  user_module
}
